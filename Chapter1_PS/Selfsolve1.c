#include <stdio.h>

int main(){
    int length, breadth;

     printf("Enter the Lenght\n");
    scanf("%d", &length);

     printf("Enter the breadth\n");
    scanf("%d",&breadth);

    printf("The AREA of REctangle is %d",length*breadth);
   
    return 0;
}